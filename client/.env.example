# Client environment reference. Copy to client/.env (Vite reads .env in the client dir).
# Used at build time; variables are inlined into the bundle - never put secrets here.

# API base URL (without the /api suffix)
VITE_BASE_URL=http://localhost:4000